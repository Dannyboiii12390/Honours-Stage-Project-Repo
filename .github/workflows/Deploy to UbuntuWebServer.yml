name: Deploy to Ubuntu Web Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: UbuntuSelfHosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
           npm install

      - name: Build project
        run: |
          npm run build

      - name: Deploy to server
        run: |
          cp -R ./dist/* /var/www/html/  # Adjust path
